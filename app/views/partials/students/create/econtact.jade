h2 Primary Emergency Contact

div.form-group
	div.input-group
		input.form-control(type='text', name="fullName1" placeholder='Full name', ng-model="model.fullName1", required, ng-maxlength='128', ng-minlength='4')
		span.input-group-addon
			span.glyphicon.glyphicon-asterisk(ng-show="createStudent.fullName1.$invalid && createStudent.fullName1.$pristine")
			span.glyphicon.glyphicon-remove(ng-show="createStudent.fullName1.$invalid && !createStudent.fullName1.$pristine")
			span.glyphicon.glyphicon-ok(ng-show="createStudent.fullName1.$valid")
	p.text-muted.error(ng-show="createStudent.fullName1.$invalid && !createStudent.fullName1.$pristine") Primary emergency contact's full name must be between 4 and 128 characters.

div.form-group.row
	div.col-md-6
		div.input-group
			input.form-control(type='tel', name="phoneNumber1" placeholder='Phone number: 555-555-5555', ng-model="model.phoneNumber1", ui-mask="{{'(999) 999-9999'}}", required)
			span.input-group-addon
				span.glyphicon.glyphicon-asterisk(ng-show="createStudent.phoneNumber1.$invalid && createStudent.phoneNumber1.$pristine")
				span.glyphicon.glyphicon-remove(ng-show="createStudent.phoneNumber1.$invalid && !createStudent.phoneNumber1.$pristine")
				span.glyphicon.glyphicon-ok(ng-show="createStudent.phoneNumber1.$valid")
		p.text-muted.error(ng-show="createStudent.phoneNumber1.$invalid && !createStudent.phoneNumber1.$pristine") Primary emergency contact's phone number must be a valid 10 digit number. E.g. 555-555-5555

	div.col-md-6
		div.input-group
			input.form-control(type='text', name="relationship1" placeholder='Relationship', ng-model="model.relationship1", typeahead="relation for relation in relations | filter:$viewValue | limitTo:5", required)
			span.input-group-addon
				span.glyphicon.glyphicon-asterisk(ng-show="createStudent.relationship1.$invalid && createStudent.relationship1.$pristine")
				span.glyphicon.glyphicon-remove(ng-show="createStudent.relationship1.$invalid && !createStudent.relationship1.$pristine")
				span.glyphicon.glyphicon-ok(ng-show="createStudent.relationship1.$valid")
		p.text-muted.error(ng-show="createStudent.relationship1.$invalid && !createStudent.relationship1.$pristine") Primary emergency contact must have some relation to the student.


h2 Secondary Emergency Contact

div.form-group
	div.input-group
		input.form-control(type='text', name="fullName2" placeholder='Full name', ng-model="model.fullName2", required, ng-maxlength='128', ng-minlength='4')
		span.input-group-addon
			span.glyphicon.glyphicon-asterisk(ng-show="createStudent.fullName2.$invalid && createStudent.fullName2.$pristine")
			span.glyphicon.glyphicon-remove(ng-show="createStudent.fullName2.$invalid && !createStudent.fullName2.$pristine")
			span.glyphicon.glyphicon-ok(ng-show="createStudent.fullName2.$valid")
	p.text-muted.error(ng-show="createStudent.fullName2.$invalid && !createStudent.fullName2.$pristine") Secondary emergency contact's full name must be between 4 and 128 characters.

div.form-group.row
	div.col-md-6
		div.input-group
			input.form-control(type='tel', name="phoneNumber2" placeholder='Phone number: 555-555-5555', ng-model="model.phoneNumber2", ui-mask="{{'(999) 999-9999'}}", required)
			span.input-group-addon
				span.glyphicon.glyphicon-asterisk(ng-show="createStudent.phoneNumber2.$invalid && createStudent.phoneNumber2.$pristine")
				span.glyphicon.glyphicon-remove(ng-show="createStudent.phoneNumber2.$invalid && !createStudent.phoneNumber2.$pristine")
				span.glyphicon.glyphicon-ok(ng-show="createStudent.phoneNumber2.$valid")
		p.text-muted.error(ng-show="createStudent.phoneNumber2.$invalid && !createStudent.phoneNumber2.$pristine") Secondary emergency contact's phone number must be a valid 10 digit number. E.g. 555-555-5555

	div.col-md-6
		div.input-group
			input.form-control(type='text', name="relationship2" placeholder='Relationship', ng-model="model.relationship2", typeahead="relation for relation in relations | filter:$viewValue | limitTo:5", required)
			span.input-group-addon
				span.glyphicon.glyphicon-asterisk(ng-show="createStudent.relationship2.$invalid && createStudent.relationship2.$pristine")
				span.glyphicon.glyphicon-remove(ng-show="createStudent.relationship2.$invalid && !createStudent.relationship2.$pristine")
				span.glyphicon.glyphicon-ok(ng-show="createStudent.relationship2.$valid")
		p.text-muted.error(ng-show="createStudent.relationship2.$invalid && !createStudent.relationship2.$pristine") Secondary emergency contact must have some relation to the student.
