h1 Create Program
br

form.form-group
	.input-group
		input(type='text', class='form-control', placeholder='Program name' ng-model="newProgram.name")
		span(class='input-group-addon')
			span(ng-show='!isEmpty(newProgram.name)', class='glyphicon glyphicon-ok input-valid')
			span(ng-show='isEmpty(newProgram.name)', class='glyphicon glyphicon-remove input-invalid')

	h2 Classes
	.row.row-margin-bottom-10
		.col-md-12
			.btn-toolbar.pull-left
				button.btn.btn-primary(type='button', ng-click='goToCreateClass()') Create new class
				button.btn.btn-danger(type='button', ng-disabled='removeClassDisabled()', ng-click='removeSelectedClasses()', ng-show='!showRemoveClassesConfirm') Remove selected
			.pull-left.margin-left-10(ng-show='showRemoveClassesConfirm && classGridOptions.selectedItems.length > 0')
				.btn-toolbar
					span.text-muted(ng-show='classGridOptions.selectedItems.length > 1') Are you sure you wish to remove the {{classGridOptions.selectedItems.length}} selected classes?
					span.text-muted(ng-show='classGridOptions.selectedItems.length == 1') Are you sure you wish to remove the selected class?
					button.btn.btn-danger(type='button', ng-click='confirmRemoveClasses(true)') Yes
					button.btn.btn-success(type='button', ng-click='confirmRemoveClasses(false)') No
	.row
		.col-md-12
			div.gridStyle(ng-grid='classGridOptions')
	h2 Ranks
	table.table
		tr
			th Name
			th Order 
			th Sub-ranks
			th Actions
		tr(ng-repeat='rank in newProgram.ranks')
			td 
				{{rank.name}}
			td
				{{rank.rankOrder}}
			td
				a(href='#') SUB-RANKS
			td
				.pull-right
					button.btn.btn-default.glyphicon.glyphicon-edit(ng-click='goToEditRank(rank)')
					button.btn.btn-danger.glyphicon.glyphicon-remove(type='button' ng-click='removeRank(rank)')
		tr
			td
				button.btn.btn-success.glyphicon.glyphicon-plus(type='button' ng-click='goToCreateRank()')
			td
			td
			td

	input.btn.btn-primary.pull-right(ng-disabled='!canSaveProgram()' type='submit', value='Create Program', ng-click='createProgram()')
	input.btn.btn-warning.pull-right(type='reset', value='Cancel', ng-click='cancelCreate()')

	div(class='clearfix')
